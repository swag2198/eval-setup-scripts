# User-specific config (generated by setup.sh, never commit)
.env.leonardo

# Python
__pycache__/
*.pyc
*.pyo

# Backup files
*.bak
*.orig

# HF cache (symlinked or too large)
hf-cache/

# Legacy scripts (kept locally for reference, not in repo)
legacy/
